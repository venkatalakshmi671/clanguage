#include<stdio.h>
#include<conio.h>
void main()
{

    int count=0,a;
    while(1)
    {
        printf("Enter a number:");
        scanf("%d",&a);
        count=count+a;
        if(a==0)
        {
            break;
        }
    }
    printf("%d",count);
}
