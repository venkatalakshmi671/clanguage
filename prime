#include<stdio.h>
void main()
{
    int a,count=0,i;
    printf("Enter a number:");
    scanf("%d",&a);
    for(i=2;i<a;i++)
    {
      if(a%i==0)
      {
        count++;
      }
    }
    if(count==0)
    {
      printf("it is prime");
    }
    else
    {
      printf("it is not a prime");
}
}
